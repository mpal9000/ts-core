import { NegativeInteger, PositiveInteger } from '../types/Integer.js'

// type-coverage:ignore-next-line
export const MINIMUM = Number.MIN_SAFE_INTEGER as NegativeInteger
// type-coverage:ignore-next-line
export const MAXIMUM = Number.MAX_SAFE_INTEGER as PositiveInteger
